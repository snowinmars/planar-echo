import type { DecompiledItem, Pathes } from 'src/shared/types.js';

const convertTwoda = (pathes: Pathes, items: DecompiledItem[]): Promise<void> => {
  return Promise.resolve();
};

export default convertTwoda;
