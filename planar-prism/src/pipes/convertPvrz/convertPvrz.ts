import type { DecompiledItem, Pathes } from 'src/shared/types.js';

const convertPvrz = (pathes: Pathes, items: DecompiledItem[]): Promise<void> => {
  return Promise.resolve();
};

export default convertPvrz;
