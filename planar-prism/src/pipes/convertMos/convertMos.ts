import type { DecompiledItem, Pathes } from 'src/shared/types.js';

const convertMos = (pathes: Pathes, items: DecompiledItem[]): Promise<void> => {
  return Promise.resolve();
};

export default convertMos;
