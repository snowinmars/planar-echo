export type RawFunction = Readonly<{
  index: number;
  offset: number;
  length: number;
  text: string;
}>;

export type DlgFunction = Readonly<{
  index: number;
  offset: number;
  length: number;
  text: string;
}>;
